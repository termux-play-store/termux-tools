if [ ! -e @TERMUX_HOME@/.config/termux/termux.properties ] && [ ! -e @TERMUX_HOME@/.termux/termux.properties ]; then
	mkdir -p @TERMUX_HOME@/.config/termux/
	cp @TERMUX_PREFIX@/share/examples/termux/termux.properties @TERMUX_HOME@/.config/termux/termux.properties
fi
